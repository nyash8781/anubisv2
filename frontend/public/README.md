# /public

This folder ships nearly empty. See `docs/PUBLIC_CLEANUP.md` for:

1. Which files to copy from `Anubis_UI/frontend/public/`
2. Which files to **delete** (template debris — do not ship)
3. What to replace before public launch (favicons, hero screenshots)

The scaffold ships with one `placeholder.svg` so the frontend builds without 404s on missing image references.
